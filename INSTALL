To build type:

  ./configure && make && make install

  In case you don't have full permissions you might want to set the prefix, doing: ./configure  --prefix=~/your/prefix/foo

To run type:

  gothenburg (perhaps ./gothenburg in your prefix)
